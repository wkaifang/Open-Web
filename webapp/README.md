# webapp

### 目录结构

```bash
```

### Install

```bash
npm install -D webpack webpack-cli webpack-dev-server webpack-merge webpack-bundle-analyzer
npm install -D style-loader css-loader file-loader url-loader
npm install -D html-webpack-plugin mini-css-extract-plugin clean-webpack-plugin
npm install -D @babel/core @babel/cli @babel/preset-env babel-loader
npm install -D eslint eslint-loader prettier eslint-config-prettier eslint-plugin-prettier
npm install -D less less-loader postcss-loader postcss-preset-env
```

### todo

```bash
```